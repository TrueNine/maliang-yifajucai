import type { ThemeConfig } from '@compose/config-theme'

export * from './BuildConfig'

export const StyleConfig: ThemeConfig = {
  primaryColor: '#ff6f00',
  secondaryColor: '#ffab00',
  light: {},
  dark: {},
}
