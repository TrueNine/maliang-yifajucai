export com.tnmaster.entities.Permissions
  -> package com.tnmaster.dto.permissions

specification PermissionsAdminSpec implements io.github.truenine.composeserver.domain.IPageParam {
  eq(id)
  eq(name)
  like/^(doc)
  o: Int?
  s: Int?
}

input PermissionsAdminPostDto {
  name
  doc
}

PermissionsView {
  id
  name
  doc
}
